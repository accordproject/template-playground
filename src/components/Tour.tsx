import { useEffect } from "react";
import { tour } from "../tour/tourConfig";
import "../styles/components/Tour.css";
const Tour = () => {
  useEffect(() => {

  }, []);

  return null;
};

export default Tour;
export { tour };
