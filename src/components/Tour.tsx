import { useEffect } from "react";
import { tour } from "../tour/tourConfig";

const Tour = () => {
  useEffect(() => {

  }, []);

  return null;
};

export default Tour;
export { tour };
